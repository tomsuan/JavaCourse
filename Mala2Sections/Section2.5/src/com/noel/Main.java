package com.noel;
/**
 * Exam Objective 2.5 Develop code that uses 
 * wrapper classes such as Boolean, Double and
 * Integer
 * @author noelf
 *Wrapper classes are in built classes that 'Wrap'
 *primitive variables in a class. Each
 *Primitive variables has a corresponding wrapper
 *class
 *an int has an Integer
 *byte has a Byte
 *short has Short
 *long has Long
 *double has Double
 *float has Float
 *char has a Character
 *boolean has a Boolean
 *remember String is NOT a primitive data type,
 *but an array of characters and its "String" with
 *a capital "S" not "string" with a lowercase "S",
 *there is no such thing as a "string" data type
 */
public class Main {
	public static void main(String args[]){
		//Examples.createWrappers();
		Examples.compareWrappers();
		
	}

}
